import { useState } from 'react';
import { Link } from 'react-router-dom';
import { api } from '../../api/client';
export function Redeem(){const [done,setDone]=useState(false),[error,setError]=useState(''),[busy,setBusy]=useState(false);return <main className="auth-panel"><h1>SONDA</h1><h2>Accept invitation / reset password</h2>{done?<p>Password set. <Link to="/">Sign in</Link></p>:<form onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);setBusy(true);try{await api('/account-grants/redeem',{method:'POST',body:{token:f.get('token'),password:f.get('password')}});setDone(true);}catch{setError('The grant could not be redeemed. It may be expired or already used.');}finally{setBusy(false);}}}><label>Invitation or reset token<input name="token" autoComplete="off" required/></label><label>New password<input name="password" type="password" autoComplete="new-password" minLength={15} maxLength={128} required/></label><button disabled={busy}>Set password</button>{error&&<p role="alert">{error}</p>}</form>}<Link to="/">Back to sign in</Link></main>;}
